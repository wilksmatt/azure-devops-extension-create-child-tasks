define([], function(){
    // Runtime configuration for Create Child Tasks extension
    // Explicit mode only. Options: 'dev' | 'release'
    // Change the value below to set logging mode.

    var mode = 'release';

    return {
        mode: mode
    };
});
